import { createGlobalStyle } from "styled-components";

const GlobalBase = createGlobalStyle`

:root {

    --white: #FFFFFF;
    --black: #000000;
    --gray: rgba(251, 251, 251);

    --btn-accept: #81d47d;
    --btn-reject: #e66868;
    --superlike-blue: #7497fb;
}

`;
export default GlobalBase;
